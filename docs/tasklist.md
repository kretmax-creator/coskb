# Coskb — План разработки

## Отчёт по прогрессу

| Итерация | Статус | Описание | Дата |
|----------|--------|----------|------|
| 0 | ✅ Готово | Структура и документация | 20.02.2026 |
| 1 | ✅ Готово | Docker Compose + сервисы | 20.02.2026 |
| 2 | ✅ Готово | Скрипты | 24.02.2026 |
| 3 | ✅ Готово | README + миграция | 24.02.2026 |
| 4 | ✅ Готово | Исследование сети | 24.02.2026 |
| 5 | ✅ Готово | CI/CD + документирование | 24.02.2026 |
| 6 | ✅ Готово | Семантический поиск (pgvector + e5-small) | 27.02.2026 |
| 7 | ✅ Готово | Гибридный поиск (FTS + vector) | 27.02.2026 |
| 8 | ✅ Готово | Telegram-бот | 27.02.2026 |
| 9 | ✅ Готово | Улучшение поиска + похожие статьи + дубликаты | 27.02.2026 |
| 10 | ✅ Готово | Synonym/alias mapping + транслитерация | 02.03.2026 |
| 11 | ✅ Готово | Автоматическое резервное копирование (еженедельное) | 03.03.2026 |
| 12 | ✅ Готово | Аналитика поиска | 03.03.2026 |
| 13 | ⬜ Не начато | CI/CD — GitHub Actions | |
| 14 | ⬜ Не начато | Просмотр логов (выбор инструмента) | |
| 15 | ⬜ Не начато | Мониторинг метрик (выбор инструмента) | |

**Легенда:** ✅ Готово | ⏳ В работе | ⬜ Не начато | 🔴 Блокер

---

## Итерация 0 — Структура и документация

**Цель:** Создать структуру проекта, документацию, конфигурацию Cursor.

- [x] Создать папку `coskb/` со структурой каталогов
- [x] Скопировать и адаптировать `.cursor/` (rules, commands, skills)
- [x] Создать `docs/vision.md` — архитектура coskb
- [x] Создать `docs/tasklist.md` — план разработки
- [x] Создать `.gitignore`
- [x] Создать `.env.example`
- [x] `git init` + первый коммит

**Проверка:** Структура создана, документация на месте, git инициализирован.

---

## Итерация 1 — Docker Compose + сервисы

**Цель:** Описать стек из трёх контейнеров.

- [x] Создать `docker-compose.yml` — postgres, wikijs, nginx
- [x] Создать `config.yml` — Wiki.js config (`offline: true`)
- [x] Создать `nginx/default.conf` — reverse proxy (port 8890 → wikijs:3000)
- [x] Настроить локализацию Wiki.js через sideload (`data/sideload`, volume в `docker-compose.yml`)

**Проверка:** `docker compose config` без ошибок; структура сервисов корректна, Wiki.js открывается по `http://<ip>:8890`, смена языка через sideload локалей работает.

---

## Итерация 2 — Скрипты

**Цель:** Скрипты для управления стеком и миграции данных.

- [x] `scripts/build.sh` — `docker pull` + `docker save` для 3 образов
- [x] `scripts/load.sh` — `docker load` из `.tar` файлов
- [x] `scripts/deploy.sh` — `docker compose up -d`
- [x] `scripts/stop.sh` — `docker compose down`
- [x] `scripts/backup_db.sh` — `pg_dump` через `docker exec`
- [x] `scripts/restore_db.sh` — восстановление дампа
- [x] `scripts/status.sh` — `docker compose ps`

**Проверка:** Скрипты содержат `set -euo pipefail`, используют `docker compose`, не выполняют разрушительных действий без подтверждения (restore).

---

## Итерация 3 — README + миграция

**Цель:** Полная документация для развёртывания и миграции.

- [x] `README.md` — требования, подготовка, деплой
- [x] README: как создать `.env` (по шаблону `.env.example`)
- [x] README: подготовка директории `/var/lib/coskb-data`
- [x] README: offline-workflow (build → load → deploy)
- [x] README: миграция данных (backup → transfer → restore)
- [x] README: проверка работоспособности
- [x] README: бэкапы (ручной запуск)
- [x] Отдельный гайд `docs/migration-xylikb.md` для первоначальной миграции из дампа `xylikb_20260220_1429.sql.migration`

**Проверка:** Новый пользователь по README и `docs/migration-xylikb.md` выполняет деплой и миграцию.

---

## Итерация 4 — Исследование сети

**Цель:** Определить сетевую топологию VM и возможности внешнего доступа к Wiki.js.

- [x] Определить внутренний и внешний IP VM (`ip addr`, `curl ifconfig.me`)
- [x] Проверить доступность порта 8890 извне корпоративной сети
- [x] Проверить доступ VM к `api.telegram.org` (`curl -s https://api.telegram.org`)
- [x] Задокументировать результаты в `docs/network-research.md`
- [x] Принять решение по доступу извне (по результатам исследования)

**Проверка:** Документ `docs/network-research.md` содержит результаты и решение.

---

## Итерация 5 — CI/CD + документирование

**Цель:** Описать процесс разработки и развёртывания, создать диаграммы архитектуры.

- [x] Описать CI/CD процесс в `docs/ci-cd.md`:
  - Модель ветвления: `master` + `feature/<name>` → PR → merge
  - Деплой: скрипт `scripts/update.sh` (git pull → docker compose build → restart)
  - Preview-деплой feature-ветки по отдельному порту (для фич без изменения схемы БД)
- [x] Создать `scripts/update.sh`
- [x] Создать Mermaid-диаграммы в `docs/diagrams/`:
  - `architecture.md` — контейнеры, сети, порты, volumes
  - `ci-cd-flow.md` — ветки → PR → merge → деплой
  - `search-sequence.md` — запрос → search-api → pgvector → результат
  - `telegram-bot-sequence.md` — сообщение → бот → search-api → ответ
- [x] Обновить `docs/vision.md` — новые компоненты (search-api, tg-bot, pgvector, services/)
- [x] Создать структуру `docs/adr/` + первый ADR (выбор pgvector + e5-small вместо Elasticsearch)
- [x] Обновить `.env.example` с новыми переменными

**Проверка:** CI/CD задокументирован, `update.sh` работает, диаграммы рендерятся на GitHub.

---

## Итерация 6 — Семантический поиск (pgvector + e5-small)

**Цель:** Развернуть семантический поиск по статьям Wiki.js.

- [x] Бэкап БД перед сменой образа (`scripts/backup_db.sh`)
- [x] Заменить `postgres:15` → `pgvector/pgvector:pg15` в `docker-compose.yml`
- [x] Init-скрипт для PostgreSQL: `CREATE EXTENSION vector;`, схема `ai`, таблица эмбеддингов
- [x] Python-сервис `search-api`:
  - `services/search-api/Dockerfile`
  - `services/search-api/requirements.txt` (fastapi, sentence-transformers, psycopg2)
  - `services/search-api/app/main.py`
  - Загрузка модели `intfloat/multilingual-e5-small`
  - `POST /index` — индексация всех статей (чтение из Wiki.js таблиц → эмбеддинги → pgvector)
  - `GET /search?q=...` — векторный поиск, top-5
- [x] Добавить сервис `search-api` в `docker-compose.yml` (внутренняя сеть, без внешних портов)
- [x] Тестирование на реальных данных Wiki.js (23 страницы проиндексированы, поиск работает)

**Проверка:** `POST /index` индексирует статьи; `GET /search?q=vpn` возвращает релевантные результаты.

---

## Итерация 7 — Гибридный поиск (FTS + vector)

**Цель:** Объединить полнотекстовый и векторный поиск для лучшего качества.

- [x] Добавить PostgreSQL FTS поиск в search-api (ts_rank по tsvector)
- [x] Взвешенное объединение: `score = α * fts_score + β * vector_score`
- [x] Веса α/β настраиваемые через env (FTS_WEIGHT=0.4, VECTOR_WEIGHT=0.6)
- [x] Обновить `GET /search` — гибридный режим по умолчанию, режимы: hybrid/vector/fts
- [x] Обновить диаграмму `docs/diagrams/search-sequence.md`
- [x] Тестирование гибридного поиска на VM (23 страницы, 3 режима работают)

**Проверка:** Гибридный поиск находит статьи и по точным ключевым словам, и по смыслу.

---

## Итерация 8 — Telegram-бот

**Цель:** Бот для поиска по Wiki.js и мониторинга здоровья сервисов.

- [x] Создать Telegram-бота через @BotFather, токен в `.env`
- [x] Python-сервис `tg-bot`:
  - `services/tg-bot/Dockerfile`
  - `services/tg-bot/requirements.txt` (python-telegram-bot)
  - `services/tg-bot/app/main.py`
  - Long polling
- [x] Команда `/search <ключевые слова>` → запрос к search-api → top 3-5 (заголовок + сниппет + ссылка)
- [x] Команда `/read <заголовок>` → полный текст статьи в Telegram
- [x] Healthcheck: периодическая проверка Wiki.js + PostgreSQL, алерт в чат при проблемах
- [x] Ограничение: бот работает только в одном указанном чате (`TELEGRAM_CHAT_ID` в `.env`)
- [x] Добавить сервис `tg-bot` в `docker-compose.yml`
- [x] Обновить `.env.example` (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`)

**Проверка:** Бот отвечает на `/search` и `/read` в указанном чате; алертит при падении сервисов.

---

## Итерация 9 — Улучшение поиска + похожие статьи + дубликаты

**Цель:** Повысить качество поиска (отсечение нерелевантных результатов) и добавить рекомендации.

- [x] Фильтрация по score: не возвращать результаты ниже порога (min_score, настраиваемый через env)
  - Три порога: `MIN_SCORE_HYBRID=0.52`, `MIN_SCORE_VECTOR=0.55`, `MIN_SCORE_FTS=0.001`
  - Нерелевантные запросы теперь возвращают пустой результат
- [x] Эндпоинт `GET /similar?page_id=...` → 3-5 похожих статей (cosine similarity)
- [x] Эндпоинт `GET /duplicates?threshold=0.9` → список потенциальных дубликатов
- [x] Команда `/similar <заголовок>` в Telegram-боте
- [x] Автотесты итерации 9: pytest `scripts/tests/test_search_iteration9.py` — 68 тестов (релевантность поиска, min_score, /similar, /duplicates, валидация)

**Проверка:** Нерелевантные запросы возвращают пустой результат; API похожих статей работает; дубликаты выявляются корректно.

---

## Итерация 10 — Synonym/alias mapping + транслитерация

**Цель:** Расширение поисковых запросов синонимами и транслитерацией для улучшения поиска по техническим терминам.

- [x] Создать `services/search-api/aliases.json` — словарь алиасов (Latin↔Cyrillic, синонимы)
- [x] Создать `services/search-api/app/aliases.py` — загрузка JSON, функция `expand_query()`
- [x] Изменить `services/search-api/app/main.py` — расширение запроса в `/search`:
  - FTS: `plainto_tsquery(original) || plainto_tsquery(rewritten)`
  - Vector: кодирование объединённого текста (original + rewritten)
  - Hybrid: оба расширения
- [x] Тестирование на VM: запросы `sacura`, `sfera`, `cvpn`, `аутлук` и др. возвращают релевантные результаты

**Проверка:** Латинские транслитерации и русские аббревиатуры находят соответствующие статьи.

---

## Итерация 11 — Автоматическое резервное копирование

**Цель:** Еженедельный автобэкап БД с ротацией старых дампов.

- [x] Создать `scripts/backup_cron.sh` — обёртка над `backup_db.sh` с ротацией (хранить N последних)
- [x] Документировать настройку cron-задачи в README
- [x] Тестирование: запуск скрипта, проверка ротации

**Проверка:** Скрипт создаёт дамп и удаляет старые при превышении лимита.

---

## Итерация 12 — Аналитика поиска

**Цель:** Логирование поисковых запросов для анализа и улучшения поиска.

- [x] Таблица `ai.search_log` (query, mode, results_count, timestamp)
- [x] Логирование каждого запроса в `/search`
- [x] Эндпоинт `GET /search-stats` — топ запросов, запросы без результатов
- [x] Команда `/stats` в Telegram-боте (или расширение существующей)

**Проверка:** Запросы логируются; статистика доступна через API и бота.

---

## Итерация 13 — CI/CD — GitHub Actions

**Цель:** Автоматические проверки качества при PR.

- [ ] GitHub Actions workflow: линтинг Python (ruff/flake8)
- [ ] Проверка `docker compose config`
- [ ] Проверка валидности `aliases.json`

**Проверка:** PR запускает checks; ошибки блокируют merge.

---

## Итерация 14 — Просмотр логов (выбор инструмента)

**Цель:** Централизованный просмотр логов сервисов.

- [ ] Исследование и выбор инструмента (Loki+Grafana / Dozzle / другое)
- [ ] Развёртывание на VM
- [ ] Документация

**Проверка:** Логи всех контейнеров доступны через веб-интерфейс.

---

## Итерация 15 — Мониторинг метрик (выбор инструмента)

**Цель:** Мониторинг здоровья сервисов и ресурсов VM.

- [ ] Исследование и выбор инструмента (Prometheus+Grafana / Uptime Kuma / другое)
- [ ] Развёртывание на VM
- [ ] Дашборд с метриками контейнеров
- [ ] Документация

**Проверка:** Дашборд отображает состояние контейнеров, CPU, память, диск.

---

## Будущее (вне текущего плана)

- Поисковый виджет в Wiki.js UI (кастомный HTML)
- Zero-downtime обновление (`scripts/update.sh` с поочерёдным рестартом)
- Chunking длинных статей (разбивка на фрагменты с перекрытием)
- RAG (вопрос-ответ по базе знаний с цитатами)
- Импорт новых данных (Этап 3 — Контент)
